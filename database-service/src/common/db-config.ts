import {ConnectionConfig} from "mysql";
 
 export let dbOptions: ConnectionConfig = {    
    host: "localhost",
    port: 3306,
    user: "myuser",
    password: "mypassword",
    database: "mydatabase"
}
